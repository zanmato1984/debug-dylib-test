# Try a weird debug issue on Mac Clion

- Open this project using Clion on Mac.
- Use Debug build type.
- Set breakpoint on `foo.cpp:4`.
- Debug program `debug-dylib-test`, see if the breakpoint triggers.
